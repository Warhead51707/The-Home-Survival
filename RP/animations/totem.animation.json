{
	"format_version": "1.8.0",
	"animations": {
		"animation.totem.idle": {
			"loop": true,
			"bones": {
				"mesh": {
					"rotation": [
						0,
						"query.anim_time * 90.0",
						0
					],
					"position": [
						0,
						"math.cos(query.anim_time * 50.0)",
						0
					]
				}
			}
		},
		"animation.totem.scale_down": {
			"loop": true,
			"bones": {
				"mesh": {
					"scale": [ "Math.clamp(1 - (query.anim_time * 0.033), 0, 1)", "Math.clamp(1 - (query.anim_time * 0.033), 0, 1)", "Math.clamp(1 - (query.anim_time * 0.033), 0, 1)"]
				}
			}
		}
	}
}